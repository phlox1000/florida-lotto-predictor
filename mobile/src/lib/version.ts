import versionConfig from "../../version.json";

export const MOBILE_VERSION = versionConfig;

export const MOBILE_BUILD_LABEL = `Build ${MOBILE_VERSION.version} (${MOBILE_VERSION.androidVersionCode})`;
